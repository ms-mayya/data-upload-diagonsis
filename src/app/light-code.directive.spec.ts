import { LightCodeDirective } from './light-code.directive';

describe('LightCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new LightCodeDirective();
    expect(directive).toBeTruthy();
  });
});
